\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\usepackage{fancyhdr}
\usepackage{geometry}
\usepackage[raggedright]{titlesec}
\usepackage[protrusion=true,expansion=true]{microtype}

\newcommand{\hflat}{\tikz [baseline] \node [anchor=base, inner sep=0pt] {\reflectbox{$\flat$}};}
\newcommand{\dsharp}{\tikz [baseline] {%
    \clip (-0.2em,-1ex) rectangle (-0.01em,2ex);
    \node[anchor=base, inner sep=0pt] {$\sharp$};}}

\newcommand{\fretboard} {\begin{scope}[xscale=-15,yscale=.3,line width=.5]

    \xdef\x{1}
    %% Left line
    \draw[line width=1.5] (1,.56) -- (1,6.44);
    \foreach \fret in {0,...,20}{
      %% Set coordinate for each string
      \foreach \str in {1,...,6}{
        \coordinate (\str-\fret) at (\x,\str);
      }
      %% Compute the position of the fret
      \pgfmathsetmacro\x{\x * 0.97193715634}
      \xdef\x{\x}
      \ifthenelse{\isodd{\fret}} {\draw (\x,.95) -- (\x,6.05);}{}
    }

    %% Draw each string
    \foreach \str in {1,...,6}{
      \draw (1,\str-.05) -- (0.97153194115*\x,\str-.05);
      \draw (1,\str+.05) -- (0.97153194115*\x,\str+.05);
    }
      \draw[line width=1.5] (1,0.65) -- (0.97153194115*\x,0.65);
      \draw[line width=1.5] (1,6.35) -- (0.97153194115*\x,6.35);
  \end{scope}
}